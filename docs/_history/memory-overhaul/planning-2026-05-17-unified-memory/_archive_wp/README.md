# Archived Work Packages

**Archived**: 2026-05-18

These 9 work-package files (`WP-A`, `WP-B`, `WP-C1`–`WP-C5`, `WP-D`, `WP-E`) were the
original delivery structure for the memory overhaul. On 2026-05-18 the plan was
re-scoped (user decision) to a **single-skill delivery model**: the entire setup is
delivered through the `memory-init` skill plus directly-applied skill edits — no work
packages.

The WP content was folded into:

- `memory-init` skill phases (scaffold, retarget, migrate, install-hook) — see
  `../OVERVIEW.md` Deliverables D1 and `../memory-init-implementation-guide.md`.
- The five direct skill edits (D4–D8).

These files are retained for the rationale trail only. **They are not active.**
They also contain the pre-correction errors (`/home/christoph/...` paths, the
fabricated 7,660-line glite figure, the "extend session-start.js" mistake) — do not
treat anything in them as fact. See `../FINDINGS.md` §"2026-05-18 Factual Corrections".
